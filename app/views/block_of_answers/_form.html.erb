<%= form_with(model: block_of_answer, local: true) do |form| %>
  <% if block_of_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(block_of_answer.errors.count, "error") %> prohibited this block_of_answer from being saved:</h2>

      <ul>
      <% block_of_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :n_option %>
    <%= form.text_field :n_option, id: :block_of_answer_n_option %>
  </div>

  <div class="field">
    <%= form.label :text_answer %>
    <%= form.text_field :text_answer, id: :block_of_answer_text_answer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
