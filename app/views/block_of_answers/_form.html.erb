<%= form_with(model: block_of_answer, local: true) do |form| %>
  <% if block_of_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(block_of_answer.errors.count, "error") %> prohibited this block_of_answer from being saved:</h2>

      <ul>
      <% block_of_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, id: :block_of_answer_text %>
  </div>

  <div class="field">
    <%= form.label :numberofoption %>
    <%= form.number_field :numberofoption, id: :block_of_answer_numberofoption %>
  </div>

  <div class="field">
    <%= form.label :answer_id %>
    <%= form.text_field :answer_id, id: :block_of_answer_answer_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
